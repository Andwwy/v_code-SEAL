# extraction pipeline (GPU pod) — exact pins for a deterministic env.
# vllm 0.10.2 pulls its own matching torch (2.8.0/cu128) and does NOT import
# torchcodec at startup (vllm>=0.11 does, and crashes on pod images missing
# libnvrtc.so.13 / FFmpeg). transformers<5 for vllm-0.10 compatibility (our
# own scripts run on 4.x and 5.x alike).
vllm==0.10.2
transformers>=4.53.2,<5
datasets>=2.19.0
accelerate>=0.30.0
tqdm
